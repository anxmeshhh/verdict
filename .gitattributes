# Shell scripts run inside Linux containers - CRLF from a Windows checkout
# would break them at the shebang. Pin LF regardless of core.autocrlf.
docker/entrypoint.sh text eol=lf
setup.sh text eol=lf
*.sh text eol=lf
